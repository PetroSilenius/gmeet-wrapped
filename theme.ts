import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    mode: 'dark',
    text: {
      primary: '#f3f6f9',
    },
    background: {
      default: '#001e3c',
    },
  },
});

export default theme;
